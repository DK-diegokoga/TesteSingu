import React, { Component } from 'react';
import { View } from 'react-native';
import { Components } from './src/Routers';

export default class App extends Component {
  render() {
    return (
      <Components />
    );
  }
}